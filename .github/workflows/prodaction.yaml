name: 'prod'
on: 
  push:
    tags:
      -  'v[0-9]+.[0-9]+.[0-9]+'
      
jobs:
  deploy:
     runs-on: self-hosted
     env:
      anun: "prod-${{  github.ref_name }}"
     steps:
       - name: 'Run Dockerhub image'
         run: |
           docker run -d -p 8080:3000 --name ${{ env.anun }}  arturgrigoryan1/news_letter:${{ github.sha }} 
